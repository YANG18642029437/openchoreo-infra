# 操作日志

- 时间：2026-07-13 16:57:32 CST
- 操作者：Codex（用户要求执行下一节点）
- 目标：把 VM121 `ocp-node-02` 的 K3s embedded etcd 数据迁移到新增 SSD 数据盘。
- 授权：本次只执行 VM121 的 snapshot、格式化、挂载、停止/启动 K3s、数据复制和健康验证；VM122 未执行。
- 变更前事实：VM121 存在唯一 19–21 GiB 空白非根磁盘；默认只读审计已通过；迁移前当前 Node 为 `Ready`。
- 运行命令：以 `--limit ocp-node-02 -e k3s_etcd_ssd_allow_format=true` 运行专用迁移 playbook，随后执行集群节点和 readyz 只读汇总检查。
- 退出码：迁移 playbook 为 0；集群健康检查为 0。
- 脱敏结果：snapshot 已创建并验证；新盘已格式化为 ext4；源目标复制统计一致；SSD 数据目录已 bind mount 到 `/var/lib/rancher/k3s/server/db`；K3s 恢复；运行时挂载、fstab bind 声明和底层 ext4 均通过。
- 受保护原始证据路径：`.private/evidence/2026-07-13-165454-ocp-node-02-k3s-etcd-ssd-migrate.txt`、`.private/evidence/2026-07-13-165730-post-vm121-cluster-health.txt`。
- SHA-256：migration `606f984462708f105fffb2bb6ee92e2a2368fc70db40970e93fe499a4439dcc0`；cluster health `58f47df15ebaee9ec8b0329e1e29c2437ed56c425b000b7290b66cfbc662e899`。
- 验证：play recap 为 `ok=35`、`changed=8`、`unreachable=0`、`failed=0`；三节点全部 `Ready`；集群 readyz 包含 `[+]etcd ok`、`[+]etcd-readiness ok` 和 `readyz check passed`。
- 回滚：未执行。VM121 当前健康；机械盘原目录已过期，回滚必须先同步 SSD 最新数据，不能直接暴露旧目录。
- 下一停止点：等待 VM122（`ocp-node-03`）格式化与迁移的执行时明确确认。

VM120、VM121 已完成 SSD 迁移；VM122 仍保持已添加但未格式化的数据盘状态。
