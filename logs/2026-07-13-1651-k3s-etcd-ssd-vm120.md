# 操作日志

- 时间：2026-07-13 16:51:21 CST
- 操作者：Codex（用户在执行时明确确认格式化并迁移 VM120）
- 目标：把 VM120 `ocp-node-01` 的 K3s embedded etcd 数据迁移到新增 SSD 数据盘。
- 授权：本次仅授权 VM120 的 snapshot、格式化、挂载、停止/启动 K3s、数据复制和健康验证；VM121、VM122 不在授权范围。
- 变更前事实：VM120 存在唯一 19–21 GiB 空白非根磁盘；默认只读审计已经通过；三节点迁移前均为 `Ready`。
- 运行命令：以 `--limit ocp-node-01 -e k3s_etcd_ssd_allow_format=true` 运行专用迁移 playbook；失败后只读复核服务、挂载和 readyz；修复等待契约后执行默认关闭格式化的幂等复验。
- 退出码：首次迁移 playbook 在最终本地 readyz 单次检查处返回失败；后续远程健康检查为 0；修复后的幂等复验为 0。
- 脱敏结果：迁移前 snapshot 已创建并验证；新盘已格式化为 ext4；源目标复制统计一致；SSD 数据目录已 bind mount 到 `/var/lib/rancher/k3s/server/db`；K3s active。首次本地 readyz 同时出现 `[+]etcd ok` 和短暂的 `[-]etcd-readiness failed`，稍后复核为 `[+]etcd ok`、`[+]etcd-readiness ok`、`readyz check passed`。
- 受保护原始证据路径：`.private/evidence/2026-07-13-164524-ocp-node-01-k3s-etcd-ssd-migrate.txt`、`.private/evidence/2026-07-13-164752-ocp-node-01-k3s-etcd-ssd-remote-health.txt`、`.private/evidence/2026-07-13-165036-ocp-node-01-k3s-etcd-ssd-idempotency.txt`。
- SHA-256：migration `b7fedbc25f984e19696a47fd87a43cccc873752d0c1f320ca13ec305789ff8b8`；remote health `fad5c86834ebf37dad95064475bfc219fb1433aa6c7867b232e433af082d9c1d`；idempotency `e8deb9bc7f4d020930636f881c6ffce713ba52820bc0716590ea85318638373f`。
- 验证：三节点均 `Ready`；VM120 K3s active；运行时挂载来源为 `/dev/sdb[/db]`；SSD 底层为 `/dev/sdb ext4`；fstab bind 声明通过；节点本地和控制机 readyz 均通过。幂等复验 `changed=0`、`unreachable=0`、`failed=0`。
- 回滚：未执行。VM120 当前健康；机械盘原目录仍保留但已经过期，若后续回滚必须按 runbook 先把 SSD 最新数据同步回系统盘，不能直接暴露旧目录。
- 下一停止点：等待 VM121（`ocp-node-02`）格式化与迁移的新的明确确认。VM122 不包含在下一次确认中。

验证器已改为 readyz 最多等待 300 秒，并同时检查运行时挂载与 fstab bind 声明。实现 HEAD 为 `3b23073c7fcce2a7a6ea1b3ffb58f7422b13a934`。
