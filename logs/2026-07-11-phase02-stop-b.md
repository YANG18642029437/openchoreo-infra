# Phase 02 Stop B 脱敏证据

- 时间：2026-07-11（Asia/Shanghai）
- 操作者：Codex 与用户
- 目标：在任何备份、删除或 Terraform apply 之前，汇总 Proxmox 与 Terraform 只读证据。
- 授权：仅执行只读 API、网络与 Terraform plan 检查；未授权远程写操作。
- 认证边界：Terraform 专用 API Token 的连接与授权范围已验证；本日志不记录 Token 标识或内容。

## 只读事实

- PVE：8.2.2。
- VM ID：130 与 9000 未被 PVE 资源列表占用；120、121、122 已被现有 VM 占用。
- Terraform 保存计划：`6 add, 0 change, 0 destroy`。
- 保存计划 SHA-256：`e2e7276a15f37374ef3dc4faaa9c35e11813b1f46c15f44069b56c3aba6e9377`。
- `XJ6T`：总计 5543.9 GiB，已用 2772.3 GiB，可用 2492.2 GiB。
- `local`：可用 209.8 GiB。
- VM120：100 GiB 系统盘与 200 GiB 数据盘。
- VM121：100 GiB 系统盘与 200 GiB 数据盘。
- VM122：100 GiB 系统盘与 200 GiB 数据盘。
- 旧 VM 的三块 200 GiB 数据盘将随 VM120、121、122 的受保护删除而释放；新的 400 GiB 数据盘只属于 VM130，不会同时挂载给三台 K3s VM。
- 本机 ARP 缓存中 `.183`、`.179`、`.176` 没有条目。这只是弱证据，不能单独证明地址未占用；执行写操作前仍需复核。

## 安全结论

- 已生成并保存 Terraform plan，尚未执行 `terraform apply`。
- 尚未运行 VM120、121、122 的 vzdump 备份。
- 尚未生成或验证本次备份的唯一私密 manifest。
- 尚未停止或删除任何现有 VM，也未修改其磁盘。
- 当前停止点：Stop B。下一步必须获得新的明确授权后才能创建备份；备份验证完成后还需再次停止并确认，才能删除旧 VM 或 apply。

## 回滚

本步骤仅产生只读证据与本地保存计划，没有远程变更，不需要现场回滚。保存计划只能作为本次审阅证据；现场状态变化后必须重新生成，禁止盲目复用。
